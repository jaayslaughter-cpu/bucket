# Optional notifications
DISCORD_WEBHOOK_URL=
# Leave blank during research-only development.

# Approved timestamped market source (OddsPapi) — research / historical CLV only.
# Never commit real keys. Rotate any key that was pasted into chat.
ODDSPAPI_API_KEY=

# ---------------------------------------------------------------------------
# TIMEZONE
# ---------------------------------------------------------------------------
# User-facing / slate dates: America/Los_Angeles only. Storage remains UTC.
TZ_DISPLAY=America/Los_Angeles

# ---------------------------------------------------------------------------
# PostgreSQL / Supabase (orchestrator main.py — optional until configured)
# ---------------------------------------------------------------------------
# Use the pooler URL (port 6543) for short jobs. Never commit real passwords.
DATABASE_URL=
# PROPIQ_MASTER_GUIDELINE=config/master_guideline_props.yaml
# BIGDATABALL_XLSX=data/external/bigdataball/2025-2026_NBA_Box_Score_Team-Stats__1_.xlsx
