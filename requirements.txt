# PropIQ Analytics — NBA only. RESEARCH_ONLY.
# Equivalent to: pip install -e ".[ml,db,dev]"

# --- core data ---
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
pyarrow>=15.0.0           # parquet copies of the outputs/ exports

# --- database (Postgres / Supabase) ---
SQLAlchemy>=2.0.30
psycopg[binary]>=3.1.18   # Postgres driver; [binary] avoids needing libpq locally
alembic>=1.13.0           # schema migrations

# --- excel / licensed exports (BigDataBall workbook) ---
openpyxl>=3.1.0

# --- modeling ---
xgboost>=2.0.0            # existing baseline
catboost>=1.2             # challenger
scikit-learn>=1.4.0       # metrics + probability calibration

# --- cli ---
typer>=0.12

# --- ingestion / http ---
requests>=2.31.0
httpx>=0.27.0

# --- validation / config ---
pydantic>=2.6.0
PyYAML>=6.0.1
python-dotenv>=1.0.1

# --- name matching ---
rapidfuzz>=3.6.0

# --- dev / test ---
pytest>=8.0.0
ruff>=0.5.0
